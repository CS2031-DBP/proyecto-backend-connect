spring.application.name=Connect
spring.datasource.url=jdbc:postgresql://Localhost:${PUERTO}/${BASE}
spring.datasource.username=${POSTGRES}
spring.datasource.password=${CONTRA}
spring.jpa.hibernate.ddl-auto=update
server.error.whitelabel.enabled=false
jwt.secret=tu_secreto_jwt
jwt.expiration=36000000000000
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${PROD_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.web.resources.add-mappings=false
